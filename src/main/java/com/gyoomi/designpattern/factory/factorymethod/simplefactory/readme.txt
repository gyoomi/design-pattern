对象的创建比较复杂的时候，就考虑使用工厂来实现。

优点：
    在简单工厂模式中，客户端不再负责对象的创建，而是把这个责任丢给了工厂类，客户端值负责对象的调用，从而明确了各个类的职责（单一职责）。
缺点：
    由于这个工厂类负责所以的对象的创建，那么当子类不断增多的时候，我们就需要去修改工厂的代码，这样就违反了开闭原则。

改良方式:
    1、把要创建的产品写在配置文件中；
    2、利用反射，动态创建；
    3、使用其他工厂模板方法。


